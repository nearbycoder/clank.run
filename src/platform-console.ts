import type { AuthRequest, DefaultAuthProfile } from "./auth.ts";

/** Renders the dependency-free deployment console. All API values enter the DOM through textContent. */
export function platformConsolePage(
  publicUrl: string,
  auth: AuthRequest<DefaultAuthProfile>,
  deviceCode: string,
  signupAllowed: boolean,
): Response {
  const nonce = randomNonce(18);
  const state = JSON.stringify({
    authenticated: Boolean(auth.user),
    email: auth.user?.email ?? null,
    csrfToken: auth.csrfToken ?? null,
    deviceCode,
    publicUrl,
    signupAllowed,
  }).replaceAll("<", "\\u003c");
  const body = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<meta name="theme-color" content="#080b12"><title>Clank Deploy</title><style>
:root{color-scheme:dark;--bg:#080b12;--panel:#10151f;--panel-2:#151b27;--line:#252d3b;--line-2:#313b4d;--text:#f4f6fb;--muted:#929cad;--faint:#697386;--lime:#c7f36a;--lime-2:#9fce43;--purple:#9c86ff;--cyan:#70d7e7;--red:#ff7d8d;--amber:#f4be5b;--radius:16px;--shadow:0 20px 70px rgba(0,0,0,.28)}
*{box-sizing:border-box}html{min-height:100%}body{margin:0;min-height:100vh;background:radial-gradient(circle at 82% -10%,rgba(105,76,220,.16),transparent 29%),var(--bg);color:var(--text);font:14px/1.5 Inter,ui-sans-serif,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input,select{font:inherit}button{color:inherit}a{color:inherit}[hidden]{display:none!important}
.noise{position:fixed;inset:0;pointer-events:none;opacity:.035;background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 160 160' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E")}
.auth-layout{min-height:100vh;display:grid;grid-template-columns:minmax(420px,1.08fr) minmax(380px,.92fr);position:relative}.auth-hero{padding:clamp(36px,7vw,96px);display:flex;flex-direction:column;justify-content:space-between;border-right:1px solid var(--line)}.brand{display:flex;align-items:center;gap:11px;font-weight:760;letter-spacing:-.02em}.brand-mark{width:29px;height:29px;display:grid;place-items:center;border-radius:9px;background:var(--lime);color:#151b0d;font-weight:950;box-shadow:0 0 32px rgba(199,243,106,.16)}.hero-copy{max-width:690px;margin:70px 0}.eyebrow{display:flex;align-items:center;gap:8px;color:var(--lime);font-size:11px;letter-spacing:.14em;text-transform:uppercase;font-weight:800}.eyebrow:before{content:"";width:7px;height:7px;background:currentColor;border-radius:50%;box-shadow:0 0 14px currentColor}.hero-copy h1{font-size:clamp(48px,7vw,92px);line-height:.95;letter-spacing:-.07em;margin:18px 0 24px;max-width:760px}.hero-copy h1 span{color:var(--muted)}.hero-copy>p{color:#adb5c4;font-size:clamp(16px,2vw,20px);max-width:580px}.proof{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.proof div{padding:16px;border:1px solid var(--line);border-radius:14px;background:rgba(16,21,31,.56)}.proof strong{display:block;font-size:13px;margin-bottom:3px}.proof span{color:var(--faint);font-size:12px}.auth-side{display:grid;place-items:center;padding:32px}.auth-card{width:min(430px,100%);background:rgba(16,21,31,.9);border:1px solid var(--line-2);border-radius:22px;padding:30px;box-shadow:var(--shadow);backdrop-filter:blur(18px)}.auth-card h2{margin:4px 0 7px;font-size:28px;letter-spacing:-.04em}.subtle{color:var(--muted)}.field{display:grid;gap:7px;margin-top:17px}.field label{color:#c7cdd8;font-size:12px;font-weight:650}.input{width:100%;border:1px solid var(--line-2);border-radius:10px;background:#0a0e16;padding:11px 12px;color:var(--text);outline:none;transition:.18s}.input:focus{border-color:var(--purple);box-shadow:0 0 0 3px rgba(156,134,255,.12)}.button{border:0;border-radius:10px;padding:10px 14px;background:var(--lime);color:#18200d;font-weight:780;cursor:pointer;transition:.16s;display:inline-flex;align-items:center;justify-content:center;gap:7px}.button:hover{background:#d3fa80;transform:translateY(-1px)}.button:disabled{opacity:.45;cursor:not-allowed;transform:none}.button.secondary{background:var(--panel-2);color:var(--text);border:1px solid var(--line-2)}.button.ghost{background:transparent;color:var(--muted);border:1px solid transparent}.button.danger{background:rgba(255,125,141,.1);color:#ff9da9;border:1px solid rgba(255,125,141,.22)}.button.small{padding:7px 10px;border-radius:8px;font-size:12px}.auth-actions{display:flex;align-items:center;gap:8px;margin-top:21px}.auth-actions .button:first-child{flex:1}.error{min-height:21px;color:#ff9da9;margin:12px 0 0;font-size:13px}
.app-shell{display:grid;grid-template-columns:238px minmax(0,1fr);min-height:100vh;position:relative}.sidebar{border-right:1px solid var(--line);background:rgba(9,12,19,.8);padding:23px 16px;display:flex;flex-direction:column;position:sticky;top:0;height:100vh}.sidebar .brand{padding:0 7px 22px}.nav-label{color:#596476;font-size:10px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;padding:14px 10px 7px}.nav-button{width:100%;border:0;background:transparent;padding:9px 11px;border-radius:9px;color:var(--muted);display:flex;align-items:center;gap:10px;cursor:pointer;text-align:left}.nav-button:hover,.nav-button.active{background:var(--panel-2);color:var(--text)}.nav-icon{width:17px;height:17px;display:grid;place-items:center;color:currentColor}.nav-spacer{flex:1}.sidebar-account{border-top:1px solid var(--line);padding:16px 8px 0}.avatar{width:30px;height:30px;border-radius:9px;background:linear-gradient(135deg,var(--purple),#6654ce);display:grid;place-items:center;font-size:11px;font-weight:850}.account-row{display:flex;align-items:center;gap:10px;min-width:0}.account-copy{min-width:0;flex:1}.account-copy strong,.account-copy span{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.account-copy span{color:var(--faint);font-size:11px}.main{min-width:0}.topbar{height:72px;border-bottom:1px solid var(--line);display:flex;align-items:center;padding:0 32px;position:sticky;top:0;background:rgba(8,11,18,.84);backdrop-filter:blur(20px);z-index:5}.breadcrumbs{color:var(--muted);display:flex;align-items:center;gap:8px}.breadcrumbs strong{color:var(--text)}.top-actions{margin-left:auto;display:flex;gap:9px}.content{padding:30px 32px 64px;max-width:1500px;margin:auto}.page-head{display:flex;align-items:flex-end;justify-content:space-between;gap:20px;margin-bottom:25px}.page-head h1{font-size:30px;line-height:1.1;letter-spacing:-.045em;margin:0 0 6px}.page-head p{margin:0;color:var(--muted)}.stat-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.stat{border:1px solid var(--line);border-radius:var(--radius);padding:18px;background:linear-gradient(145deg,rgba(21,27,39,.82),rgba(13,17,26,.82));min-width:0}.stat-label{color:var(--muted);font-size:12px;display:flex;justify-content:space-between}.stat-value{font-size:29px;letter-spacing:-.045em;font-weight:720;margin:13px 0 2px}.stat-meta{color:var(--faint);font-size:11px}.trend{color:var(--lime)}.grid-2{display:grid;grid-template-columns:minmax(0,1.6fr) minmax(290px,.6fr);gap:14px;margin-top:14px}.panel{border:1px solid var(--line);border-radius:var(--radius);background:rgba(16,21,31,.72);overflow:hidden}.panel-head{padding:17px 19px;border-bottom:1px solid var(--line);display:flex;align-items:center;gap:12px}.panel-head h2{font-size:14px;margin:0}.panel-head .subtle{font-size:11px;margin-left:auto}.panel-body{padding:18px}.quota-number{font-size:34px;font-weight:720;letter-spacing:-.05em}.quota-number span{font-size:15px;color:var(--faint);font-weight:500}.progress{height:7px;background:#242b38;border-radius:99px;overflow:hidden;margin:15px 0 10px}.progress span{height:100%;display:block;background:linear-gradient(90deg,var(--lime-2),var(--lime));border-radius:inherit;transition:width .3s}.project-list{display:grid}.project-row{border:0;border-bottom:1px solid var(--line);background:transparent;color:inherit;display:grid;grid-template-columns:minmax(180px,1.2fr) .55fr .55fr .55fr 24px;gap:14px;align-items:center;padding:15px 19px;text-align:left;cursor:pointer}.project-row:last-child{border-bottom:0}.project-row:hover{background:rgba(255,255,255,.018)}.project-name{display:flex;align-items:center;gap:11px;min-width:0}.project-glyph{width:35px;height:35px;border:1px solid var(--line-2);border-radius:10px;background:#0b1018;display:grid;place-items:center;color:var(--lime);font-weight:850}.project-copy{min-width:0}.project-copy strong,.project-copy span{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.project-copy span{font-size:11px;color:var(--faint)}.cell-label{display:block;color:var(--faint);font-size:10px;text-transform:uppercase;letter-spacing:.07em}.cell-value{font-size:12px}.status{display:inline-flex;align-items:center;gap:6px;font-size:11px;color:var(--muted)}.status:before{content:"";width:6px;height:6px;border-radius:50%;background:var(--faint)}.status.online{color:#c8eda0}.status.online:before{background:var(--lime);box-shadow:0 0 9px rgba(199,243,106,.5)}.status.degraded{color:#ffd58b}.status.degraded:before{background:var(--amber)}.empty{padding:45px 24px;text-align:center;color:var(--muted)}.empty strong{display:block;color:var(--text);font-size:16px;margin-bottom:5px}
.project-hero{display:flex;align-items:flex-start;gap:15px}.project-hero .project-glyph{width:46px;height:46px;font-size:17px}.project-title h1{margin:0;font-size:27px;letter-spacing:-.04em}.project-title a{color:var(--muted);text-decoration:none;font-size:12px}.project-title a:hover{color:var(--lime)}.tabs{display:flex;gap:3px;border-bottom:1px solid var(--line);margin:24px 0 18px}.tab{border:0;border-bottom:2px solid transparent;background:transparent;color:var(--muted);padding:10px 13px;cursor:pointer}.tab.active{color:var(--text);border-color:var(--lime)}.metric-toolbar{display:flex;align-items:center;gap:5px}.range{border:1px solid transparent;border-radius:7px;background:transparent;color:var(--muted);padding:5px 8px;cursor:pointer;font-size:11px}.range.active{background:var(--panel-2);border-color:var(--line);color:var(--text)}.chart-wrap{height:250px;padding:11px 5px 0}.chart{width:100%;height:100%;overflow:visible}.chart-grid{stroke:#252d3b;stroke-width:1}.chart-bar{fill:rgba(156,134,255,.22)}.chart-line{fill:none;stroke:var(--lime);stroke-width:2.2;stroke-linejoin:round;stroke-linecap:round}.chart-area{fill:url(#area)}.chart-empty{display:grid;place-items:center;height:100%;color:var(--faint)}.legend{display:flex;gap:16px;color:var(--faint);font-size:11px}.legend i{width:8px;height:8px;border-radius:2px;display:inline-block;margin-right:5px}.table-wrap{overflow:auto}.table{width:100%;border-collapse:collapse;min-width:650px}.table th,.table td{text-align:left;padding:12px 16px;border-bottom:1px solid var(--line);font-size:12px}.table th{color:var(--faint);font-size:10px;text-transform:uppercase;letter-spacing:.08em;font-weight:650}.table tr:last-child td{border-bottom:0}.mono{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:11px}.domain-stack{display:grid;gap:12px}.domain-card{border:1px solid var(--line);border-radius:13px;background:#0b1018;padding:16px}.domain-card-head{display:flex;align-items:center;gap:10px}.domain-card-head strong{font-size:13px}.domain-actions{margin-left:auto;display:flex;gap:6px}.step-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin-top:13px}.dns-step{background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:12px}.dns-step-label{display:flex;align-items:center;gap:7px;color:var(--muted);font-size:11px;margin-bottom:8px}.dns-step-label b{width:18px;height:18px;border-radius:50%;display:grid;place-items:center;background:var(--line);color:var(--text);font-size:9px}.dns-record{display:grid;grid-template-columns:52px minmax(0,1fr);gap:5px}.dns-record span{color:var(--faint);font-size:10px}.dns-record code{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.tls-copy{color:var(--faint);font-size:11px;margin:0}.logs{background:#080b11;border-radius:10px;border:1px solid var(--line);padding:12px;height:320px;overflow:auto;color:#b9c4d5}.log-line{display:grid;grid-template-columns:74px 58px minmax(0,1fr);gap:9px;margin-bottom:5px;font:11px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace}.log-line time{color:#5e6b80}.log-line b{color:var(--purple);font-weight:500}.log-line span{white-space:pre-wrap;overflow-wrap:anywhere}
.banner{border:1px solid rgba(156,134,255,.3);background:linear-gradient(90deg,rgba(156,134,255,.12),rgba(112,215,231,.06));border-radius:13px;padding:14px 16px;margin-bottom:18px;display:flex;align-items:center;gap:14px}.banner-copy{flex:1}.banner-copy strong{display:block}.banner-copy span{color:var(--muted);font-size:12px}.code{font:700 14px ui-monospace,monospace;color:var(--lime)}dialog{border:1px solid var(--line-2);background:var(--panel);color:var(--text);border-radius:18px;padding:0;width:min(500px,calc(100vw - 32px));box-shadow:var(--shadow)}dialog::backdrop{background:rgba(2,4,8,.72);backdrop-filter:blur(5px)}.dialog-head{padding:20px 22px 14px}.dialog-head h2{margin:0 0 5px;font-size:20px;letter-spacing:-.03em}.dialog-body{padding:0 22px 20px}.dialog-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:20px}.toast-region{position:fixed;right:20px;bottom:20px;z-index:30;display:grid;gap:8px}.toast{max-width:370px;background:#19202d;border:1px solid var(--line-2);border-radius:11px;padding:11px 14px;box-shadow:var(--shadow);animation:toast .2s ease-out}.toast.error-toast{border-color:rgba(255,125,141,.38)}@keyframes toast{from{transform:translateY(8px);opacity:0}}.mobile-menu{display:none}
@media(max-width:1000px){.auth-layout{grid-template-columns:1fr}.auth-hero{display:none}.stat-grid{grid-template-columns:repeat(2,1fr)}.grid-2{grid-template-columns:1fr}.project-row{grid-template-columns:minmax(170px,1fr) .6fr .6fr 20px}.project-row>div:nth-child(4){display:none}}
@media(max-width:720px){.app-shell{display:block}.sidebar{position:fixed;left:0;z-index:20;width:238px;transform:translateX(-102%);transition:.2s}.sidebar.open{transform:none}.mobile-menu{display:inline-flex}.topbar{padding:0 17px}.content{padding:24px 16px 54px}.page-head{align-items:flex-start;flex-direction:column}.stat-grid{grid-template-columns:1fr 1fr}.stat-value{font-size:24px}.project-row{grid-template-columns:minmax(0,1fr) 75px 18px;padding:14px}.project-row>div:nth-child(3),.project-row>div:nth-child(4){display:none}.step-grid{grid-template-columns:1fr}.proof{grid-template-columns:1fr}.auth-side{padding:18px}.auth-card{padding:23px}}
@media(max-width:430px){.stat-grid{grid-template-columns:1fr}.top-actions .button span{display:none}}
</style></head><body><div class="noise"></div>
<section class="auth-layout" id="auth-view">
  <div class="auth-hero"><div class="brand"><span class="brand-mark">C</span> Clank Deploy</div>
    <div class="hero-copy"><p class="eyebrow">Open-source application cloud</p><h1>Ship fast.<br><span>See everything.</span></h1><p>Deploy deterministic releases, watch every request, and connect customer domains without hiding the machinery.</p></div>
    <div class="proof"><div><strong>Zero dependencies</strong><span>Node, SQLite, Web Crypto</span></div><div><strong>Live by default</strong><span>Realtime state across sessions</span></div><div><strong>Auditable</strong><span>Checksummed releases and migrations</span></div></div>
  </div>
  <div class="auth-side"><div class="auth-card"><p class="eyebrow">Control plane access</p><h2 id="auth-title">Sign in</h2><p class="subtle" id="auth-subtitle">Manage your sites, traffic, and domains.</p>
    <form id="auth-form"><div class="field"><label for="email">Email</label><input class="input" id="email" type="email" autocomplete="email" required></div>
    <div class="field"><label for="password">Password</label><input class="input" id="password" type="password" autocomplete="current-password" minlength="12" required></div>
    <div class="field" id="name-field" hidden><label for="name">Name</label><input class="input" id="name" autocomplete="name"></div>
    <p class="error" id="auth-error" role="alert"></p><div class="auth-actions"><button class="button" id="auth-submit" type="submit">Sign in</button><button class="button ghost" id="auth-switch" type="button">Create account</button></div></form>
  </div></div>
</section>
<section class="app-shell" id="app-view" hidden>
  <aside class="sidebar" id="sidebar"><div class="brand"><span class="brand-mark">C</span> Clank</div><div class="nav-label">Workspace</div>
    <button class="nav-button active" id="nav-overview"><span class="nav-icon">◫</span>Overview</button><div class="nav-label">Sites</div><div id="site-nav"></div><div class="nav-spacer"></div>
    <div class="sidebar-account"><div class="account-row"><span class="avatar" id="avatar">NC</span><div class="account-copy"><strong id="account-name">Account</strong><span id="account-email"></span></div><button class="button ghost small" id="sign-out" title="Sign out">↗</button></div></div>
  </aside>
  <main class="main"><header class="topbar"><button class="button ghost small mobile-menu" id="mobile-menu">☰</button><div class="breadcrumbs"><span>Workspace</span><span>/</span><strong id="crumb">Overview</strong></div><div class="top-actions"><button class="button secondary small" id="refresh"><span>Refresh</span> ↻</button><button class="button small" id="new-site"><span>New site</span> ＋</button></div></header>
    <div class="content"><div id="device-banner" class="banner" hidden><div class="banner-copy"><strong>CLI authorization request</strong><span><span id="client-name">Clank CLI</span> is asking for code <span class="code" id="device-code"></span></span></div><button class="button small" id="approve">Authorize</button><button class="button secondary small" id="deny">Deny</button></div>
      <section id="overview-page"><div class="page-head"><div><h1>Good to see you.</h1><p>Here is what your applications are doing right now.</p></div><p class="subtle" id="last-updated"></p></div>
        <div class="stat-grid"><article class="stat"><div class="stat-label">Sites <span>◆</span></div><div class="stat-value" id="stat-sites">—</div><div class="stat-meta" id="stat-sites-meta">Across your workspaces</div></article>
        <article class="stat"><div class="stat-label">Requests <span class="trend">↗</span></div><div class="stat-value" id="stat-requests">—</div><div class="stat-meta">Last 24 hours</div></article>
        <article class="stat"><div class="stat-label">Error rate <span>◎</span></div><div class="stat-value" id="stat-errors">—</div><div class="stat-meta">5xx responses · 24 hours</div></article>
        <article class="stat"><div class="stat-label">Bandwidth <span>⇅</span></div><div class="stat-value" id="stat-bandwidth">—</div><div class="stat-meta">Known ingress bytes · 24 hours</div></article></div>
        <div class="grid-2"><article class="panel"><div class="panel-head"><h2>Your sites</h2><span class="subtle" id="online-count"></span></div><div class="project-list" id="project-list"></div></article>
        <aside class="panel"><div class="panel-head"><h2>Plan usage</h2><span class="subtle">Enforced</span></div><div class="panel-body"><div class="quota-number" id="quota-number">—</div><div class="progress"><span id="quota-bar" style="width:0"></span></div><p class="subtle" id="quota-copy">Loading workspace limits…</p></div></aside></div>
      </section>
      <section id="project-page" hidden><div class="page-head"><div class="project-hero"><span class="project-glyph" id="project-glyph">A</span><div class="project-title"><h1 id="project-name">Site</h1><a id="project-url" target="_blank" rel="noreferrer"></a></div></div><div class="top-actions"><span class="status" id="project-status"></span></div></div>
        <div class="tabs"><button class="tab active" data-tab="performance">Performance</button><button class="tab" data-tab="domains">Domains</button><button class="tab" data-tab="deployments">Deployments</button><button class="tab" data-tab="logs">Logs</button></div>
        <div id="tab-performance"><div class="stat-grid"><article class="stat"><div class="stat-label">Requests</div><div class="stat-value" id="metric-requests">—</div><div class="stat-meta" id="metric-rpm">—</div></article><article class="stat"><div class="stat-label">P95 latency</div><div class="stat-value" id="metric-p95">—</div><div class="stat-meta">Fixed, aggregatable histogram</div></article><article class="stat"><div class="stat-label">Average latency</div><div class="stat-value" id="metric-average">—</div><div class="stat-meta">Edge-to-upstream response</div></article><article class="stat"><div class="stat-label">Server errors</div><div class="stat-value" id="metric-errors">—</div><div class="stat-meta">HTTP 5xx responses</div></article></div>
          <article class="panel" style="margin-top:14px"><div class="panel-head"><h2>Traffic and latency</h2><div class="legend"><span><i style="background:rgba(156,134,255,.55)"></i>Requests</span><span><i style="background:var(--lime)"></i>P95 latency</span></div><div class="metric-toolbar" id="range-toolbar"><button class="range" data-range="1h">1h</button><button class="range active" data-range="24h">24h</button><button class="range" data-range="7d">7d</button><button class="range" data-range="30d">30d</button></div></div><div class="panel-body chart-wrap" id="chart-wrap"></div></article>
        </div>
        <div id="tab-domains" hidden><article class="panel"><div class="panel-head"><h2>Custom domains</h2><span class="subtle" id="domain-quota"></span><button class="button small" id="add-domain">Add domain</button></div><div class="panel-body"><div class="domain-stack" id="domain-list"></div></div></article></div>
        <div id="tab-deployments" hidden><article class="panel"><div class="panel-head"><h2>Deployments</h2><span class="subtle">Immutable release history</span></div><div class="table-wrap"><table class="table"><thead><tr><th>Release</th><th>Status</th><th>Framework</th><th>Artifact</th><th>Created</th></tr></thead><tbody id="release-list"></tbody></table></div></article></div>
        <div id="tab-logs" hidden><article class="panel"><div class="panel-head"><h2>Runtime logs</h2><span class="subtle">Latest 100 · secret values redacted</span></div><div class="panel-body"><div class="logs" id="log-list"></div></div></article></div>
      </section>
    </div>
  </main>
</section>
<dialog id="site-dialog"><form method="dialog" id="site-form"><div class="dialog-head"><h2>Create a new site</h2><p class="subtle">A private database and deployment history are provisioned automatically.</p></div><div class="dialog-body"><div class="field"><label for="site-name">Site name</label><input class="input" id="site-name" required maxlength="100" placeholder="Acme Tasks"></div><div class="field"><label for="site-slug">URL slug</label><input class="input" id="site-slug" maxlength="48" placeholder="acme-tasks"></div><div class="field"><label for="site-organization">Workspace</label><select class="input" id="site-organization"></select></div><p class="error" id="site-error"></p><div class="dialog-actions"><button class="button secondary" value="cancel" type="button" data-close="site-dialog">Cancel</button><button class="button" type="submit">Create site</button></div></div></form></dialog>
<dialog id="domain-dialog"><form method="dialog" id="domain-form"><div class="dialog-head"><h2>Connect a custom domain</h2><p class="subtle">Clank verifies ownership first, then confirms the hostname routes to your edge.</p></div><div class="dialog-body"><div class="field"><label for="domain-hostname">Hostname</label><input class="input" id="domain-hostname" required maxlength="253" placeholder="app.example.com"></div><p class="subtle">For apex domains, use your DNS provider's ALIAS/ANAME flattening or the displayed A/AAAA records.</p><p class="error" id="domain-error"></p><div class="dialog-actions"><button class="button secondary" type="button" data-close="domain-dialog">Cancel</button><button class="button" type="submit">Add domain</button></div></div></form></dialog>
<div class="toast-region" id="toasts" aria-live="polite"></div>
<script nonce="${nonce}">const initial=${state};
(function(){"use strict";
const state={initial:initial,dashboard:null,currentProject:null,projectData:null,range:"24h",register:false,refreshTimer:null};
const q=(selector,root=document)=>root.querySelector(selector);const qa=(selector,root=document)=>Array.from(root.querySelectorAll(selector));
const el=(tag,className,text)=>{const node=document.createElement(tag);if(className)node.className=className;if(text!==undefined)node.textContent=String(text);return node};
const clear=(node)=>{while(node.firstChild)node.removeChild(node.firstChild)};
function toast(message,error){const node=el("div","toast"+(error?" error-toast":""),message);q("#toasts").append(node);setTimeout(()=>node.remove(),4200)}
function formatNumber(value){return new Intl.NumberFormat(undefined,{notation:Number(value)>=10000?"compact":"standard",maximumFractionDigits:1}).format(Number(value)||0)}
function formatPercent(value){return ((Number(value)||0)*100).toFixed(value&&value<.001?2:1)+"%"}
function formatBytes(value){let size=Number(value)||0;const units=["B","KB","MB","GB","TB"];let index=0;while(size>=1024&&index<units.length-1){size/=1024;index++}return (index?size.toFixed(size>=10?1:2):Math.round(size))+" "+units[index]}
function formatLatency(value){const amount=Number(value)||0;return amount>=1000?(amount/1000).toFixed(2)+" s":Math.round(amount)+" ms"}
function formatDate(value){return value?new Intl.DateTimeFormat(undefined,{month:"short",day:"numeric",hour:"numeric",minute:"2-digit"}).format(new Date(value)):"—"}
function initials(email){return String(email||"C").split("@")[0].split(/[ ._-]+/).map(x=>x[0]).join("").slice(0,2).toUpperCase()||"C"}
async function api(path,options){options=options||{};const method=options.method||"GET";const headers={};if(options.body!==undefined)headers["content-type"]="application/json";if(!["GET","HEAD","OPTIONS"].includes(method)&&initial.csrfToken)headers["x-clank-csrf"]=initial.csrfToken;const response=await fetch(path,{method:method,headers:headers,body:options.body===undefined?undefined:JSON.stringify(options.body)});let data={};try{data=await response.json()}catch{}if(!response.ok){const error=new Error(data.error&&data.error.message||"Request failed with "+response.status+".");error.code=data.error&&data.error.code;error.status=response.status;throw error}return data}
function showAuthenticated(authenticated){initial.authenticated=authenticated;q("#auth-view").hidden=authenticated;q("#app-view").hidden=!authenticated;if(authenticated){q("#account-email").textContent=initial.email||"";q("#account-name").textContent=(initial.email||"Account").split("@")[0];q("#avatar").textContent=initials(initial.email)}}
function startRefresh(){clearInterval(state.refreshTimer);state.refreshTimer=setInterval(()=>{if(state.currentProject)loadProject();else loadDashboard(true)},30000)}
function returnToSignIn(){clearInterval(state.refreshTimer);state.refreshTimer=null;initial.authenticated=false;initial.csrfToken=null;initial.email=null;state.dashboard=null;state.currentProject=null;state.projectData=null;showAuthenticated(false);state.register=false;updateAuth()}
function handleAuthFailure(error){if(error&&error.status===401){returnToSignIn();return true}return false}
function updateAuth(){q("#auth-title").textContent=state.register?"Create your account":"Sign in";q("#auth-subtitle").textContent=state.register?"Create the first control-plane owner.":"Manage your sites, traffic, and domains.";q("#name-field").hidden=!state.register;q("#auth-submit").textContent=state.register?"Create account":"Sign in";q("#auth-switch").textContent=state.register?"Use existing account":"Create account";q("#auth-switch").hidden=!initial.signupAllowed}
q("#auth-switch").onclick=()=>{state.register=!state.register;updateAuth()};
q("#auth-form").onsubmit=async event=>{event.preventDefault();q("#auth-error").textContent="";const submit=q("#auth-submit");submit.disabled=true;try{const data=await api("/__clank/auth/"+(state.register?"register":"login"),{method:"POST",body:{email:q("#email").value,password:q("#password").value,...(state.register?{profile:{name:q("#name").value||undefined}}:{})}});initial.authenticated=true;initial.email=data.user.email;initial.csrfToken=data.csrfToken;showAuthenticated(true);startRefresh();await Promise.all([loadDashboard(),loadDevice()])}catch(error){q("#auth-error").textContent=error.message}finally{submit.disabled=false}};
q("#sign-out").onclick=async()=>{try{await api("/__clank/auth/logout",{method:"POST",body:{}});returnToSignIn()}catch(error){if(!handleAuthFailure(error))toast(error.message,true)}};
async function loadDevice(){if(!initial.authenticated||!initial.deviceCode)return;try{const data=await api("/api/device/info?code="+encodeURIComponent(initial.deviceCode));q("#device-banner").hidden=false;q("#device-code").textContent=data.code;q("#client-name").textContent=data.clientName}catch(error){toast(error.message,true)}}
async function decide(action){try{await api("/api/device/"+action,{method:"POST",body:{code:initial.deviceCode}});q("#device-banner").hidden=true;toast(action==="approve"?"CLI authorized.":"CLI request denied.")}catch(error){toast(error.message,true)}}
q("#approve").onclick=()=>decide("approve");q("#deny").onclick=()=>decide("deny");
async function loadDashboard(silent){try{const data=await api("/api/dashboard");state.dashboard=data;renderDashboard();if(!silent)toast("Dashboard is up to date.")}catch(error){if(!handleAuthFailure(error)&&!silent)toast(error.message,true)}}
function renderDashboard(){const data=state.dashboard;if(!data)return;q("#stat-sites").textContent=formatNumber(data.totals.projects);q("#stat-sites-meta").textContent=data.totals.online+" online";q("#stat-requests").textContent=formatNumber(data.totals.requests);q("#stat-errors").textContent=formatPercent(data.totals.errorRate);q("#stat-bandwidth").textContent=formatBytes(data.totals.bandwidthBytes);q("#online-count").textContent=data.totals.online+" of "+data.totals.projects+" online";q("#last-updated").textContent="Updated "+new Intl.DateTimeFormat(undefined,{hour:"numeric",minute:"2-digit",second:"2-digit"}).format(new Date());renderProjectList(data.projects);renderSiteNavigation(data.projects);renderQuota(data);renderOrganizationOptions(data.organizations)}
function renderQuota(data){const used=data.organizations.reduce((sum,org)=>sum+org.usage.projects,0);const total=data.organizations.reduce((sum,org)=>sum+org.usage.limit,0)||data.limits.projectsPerOrganization;q("#quota-number").textContent=used+" / ";const span=el("span",null,total+" sites");q("#quota-number").append(span);q("#quota-bar").style.width=Math.min(100,used/Math.max(1,total)*100)+"%";q("#quota-copy").textContent=(total-used)+" site slots available across "+data.organizations.length+" workspace"+(data.organizations.length===1?"":"s")+"."}
function renderProjectList(projects){const list=q("#project-list");clear(list);if(!projects.length){const empty=el("div","empty");empty.append(el("strong",null,"No sites yet"),el("span",null,"Create one here or run clank deploy from a project."));list.append(empty);return}projects.forEach(project=>{const row=el("button","project-row");row.type="button";const name=el("div","project-name");name.append(el("span","project-glyph",project.name[0].toUpperCase()));const copy=el("span","project-copy");copy.append(el("strong",null,project.name),el("span",null,project.url));name.append(copy);const statusCell=el("div");statusCell.append(el("span","cell-label","Status"),el("span","status "+project.runtimeStatus,project.runtimeStatus.replace("_"," ")));const requests=el("div");requests.append(el("span","cell-label","Requests"),el("span","cell-value",formatNumber(project.metrics.requests)));const latency=el("div");latency.append(el("span","cell-label","P95"),el("span","cell-value",formatLatency(project.metrics.p95LatencyMs)));row.append(name,statusCell,requests,latency,el("span",null,"›"));row.onclick=()=>openProject(project.id);list.append(row)})}
function renderSiteNavigation(projects){const nav=q("#site-nav");clear(nav);projects.slice(0,12).forEach(project=>{const button=el("button","nav-button",project.name);button.onclick=()=>openProject(project.id);button.dataset.project=project.id;nav.append(button)})}
function renderOrganizationOptions(organizations){const select=q("#site-organization");clear(select);organizations.forEach(org=>{const option=el("option",null,org.name+" · "+org.usage.projects+"/"+org.usage.limit);option.value=org.id;option.disabled=org.usage.projects>=org.usage.limit;select.append(option)})}
function showOverview(){state.currentProject=null;q("#overview-page").hidden=false;q("#project-page").hidden=true;q("#crumb").textContent="Overview";q("#nav-overview").classList.add("active");qa("[data-project]").forEach(node=>node.classList.remove("active"));loadDashboard(true)}
q("#nav-overview").onclick=showOverview;
async function openProject(id){state.currentProject=id;q("#overview-page").hidden=true;q("#project-page").hidden=false;q("#nav-overview").classList.remove("active");qa("[data-project]").forEach(node=>node.classList.toggle("active",node.dataset.project===id));await loadProject()}
async function loadProject(){if(!state.currentProject)return;try{const id=encodeURIComponent(state.currentProject);const [detail,releases,domains,logs,metrics]=await Promise.all([api("/api/projects/"+id),api("/api/projects/"+id+"/releases"),api("/api/projects/"+id+"/domains"),api("/api/projects/"+id+"/logs?limit=100"),api("/api/projects/"+id+"/metrics?range="+state.range)]);state.projectData={detail:detail,releases:releases.releases,domains:domains.domains,domainLimit:domains.limit,logs:logs.logs,metrics:metrics};renderProject()}catch(error){if(!handleAuthFailure(error)){toast(error.message,true);showOverview()}}}
function renderProject(){const data=state.projectData;if(!data)return;const project=data.detail.project;q("#crumb").textContent=project.name;q("#project-name").textContent=project.name;q("#project-glyph").textContent=project.name[0].toUpperCase();q("#project-url").textContent=project.url;q("#project-url").href=project.url;const status=q("#project-status");status.className="status "+project.runtimeStatus;status.textContent=project.runtimeStatus.replace("_"," ");renderMetrics(data.metrics);renderDomains(data.domains,data.domainLimit);renderReleases(data.releases);renderLogs(data.logs)}
function renderMetrics(data){const summary=data.summary;q("#metric-requests").textContent=formatNumber(summary.requests);q("#metric-rpm").textContent=summary.requestsPerMinute.toFixed(summary.requestsPerMinute<1?2:1)+" requests / min";q("#metric-p95").textContent=formatLatency(summary.p95LatencyMs);q("#metric-average").textContent=formatLatency(summary.averageLatencyMs);q("#metric-errors").textContent=formatPercent(summary.errorRate);qa("[data-range]").forEach(node=>node.classList.toggle("active",node.dataset.range===data.range));renderChart(data.points)}
function renderChart(points){const wrap=q("#chart-wrap");clear(wrap);if(!points.length||!points.some(point=>point.requests)){wrap.append(el("div","chart-empty","Traffic will appear here after requests pass through managed ingress."));return}const width=900,height=220,pad=16,innerH=height-pad*2;const maxRequests=Math.max(1,...points.map(point=>point.requests));const maxLatency=Math.max(1,...points.map(point=>point.p95LatencyMs));const svg=document.createElementNS("http://www.w3.org/2000/svg","svg");svg.setAttribute("viewBox","0 0 "+width+" "+height);svg.setAttribute("class","chart");const defs=document.createElementNS(svg.namespaceURI,"defs");const gradient=document.createElementNS(svg.namespaceURI,"linearGradient");gradient.id="area";gradient.setAttribute("x1","0");gradient.setAttribute("y1","0");gradient.setAttribute("x2","0");gradient.setAttribute("y2","1");[["0%","#c7f36a",".18"],["100%","#c7f36a","0"]].forEach(item=>{const stop=document.createElementNS(svg.namespaceURI,"stop");stop.setAttribute("offset",item[0]);stop.setAttribute("stop-color",item[1]);stop.setAttribute("stop-opacity",item[2]);gradient.append(stop)});defs.append(gradient);svg.append(defs);for(let i=0;i<4;i++){const line=document.createElementNS(svg.namespaceURI,"line");const y=pad+i*innerH/3;line.setAttribute("x1",pad);line.setAttribute("x2",width-pad);line.setAttribute("y1",y);line.setAttribute("y2",y);line.setAttribute("class","chart-grid");svg.append(line)}const step=(width-pad*2)/Math.max(1,points.length);points.forEach((point,index)=>{const rect=document.createElementNS(svg.namespaceURI,"rect");const barH=point.requests/maxRequests*innerH;rect.setAttribute("x",String(pad+index*step+step*.17));rect.setAttribute("y",String(height-pad-barH));rect.setAttribute("width",String(Math.max(1,step*.66)));rect.setAttribute("height",String(barH));rect.setAttribute("rx","2");rect.setAttribute("class","chart-bar");svg.append(rect)});const linePoints=points.map((point,index)=>{const x=pad+(index+.5)*step;const y=height-pad-point.p95LatencyMs/maxLatency*innerH;return x+","+y});const area=document.createElementNS(svg.namespaceURI,"path");area.setAttribute("d","M "+linePoints[0]+" L "+linePoints.join(" L ")+" L "+(pad+(points.length-.5)*step)+","+(height-pad)+" L "+(pad+.5*step)+","+(height-pad)+" Z");area.setAttribute("class","chart-area");svg.append(area);const polyline=document.createElementNS(svg.namespaceURI,"polyline");polyline.setAttribute("points",linePoints.join(" "));polyline.setAttribute("class","chart-line");svg.append(polyline);wrap.append(svg)}
qa("[data-range]").forEach(button=>button.onclick=async()=>{state.range=button.dataset.range;await loadProject()});
qa(".tab").forEach(button=>button.onclick=()=>{qa(".tab").forEach(tab=>tab.classList.toggle("active",tab===button));["performance","domains","deployments","logs"].forEach(name=>q("#tab-"+name).hidden=name!==button.dataset.tab)});
function renderDomains(domains,limit){q("#domain-quota").textContent=domains.length+" of "+limit+" domains";const list=q("#domain-list");clear(list);if(!domains.length){const empty=el("div","empty");empty.append(el("strong",null,"Use your own hostname"),el("span",null,"Ownership, routing, and TLS eligibility are tracked independently."));list.append(empty);return}domains.forEach(domain=>{const card=el("article","domain-card");const head=el("div","domain-card-head");head.append(el("span","status "+(domain.status==="ready"?"online":"degraded"),domain.hostname));const actions=el("div","domain-actions");const check=el("button","button secondary small","Check DNS");check.onclick=()=>domainAction(domain,"check");const verify=el("button","button small",domain.ownership.status==="verified"?"Verified":"Verify ownership");verify.disabled=domain.ownership.status==="verified";verify.onclick=()=>domainAction(domain,"verify");const remove=el("button","button danger small","Remove");remove.onclick=()=>removeDomain(domain);actions.append(check,verify,remove);head.append(actions);card.append(head);const steps=el("div","step-grid");steps.append(dnsStep("1","Ownership · "+domain.ownership.status,domain.ownership.record));const route=domain.routing.recommendedRecord||{type:"A/AAAA",name:domain.hostname,value:domain.routing.edgeAddresses.join(", ")||"Operator configuration required"};steps.append(dnsStep("2","Routing · "+domain.routing.status,route),tlsStep(domain.certificate.status));card.append(steps);if(domain.routing.error)card.append(el("p","error",domain.routing.error));list.append(card)})}
function dnsStep(number,label,record){const step=el("div","dns-step");const heading=el("div","dns-step-label");heading.append(el("b",null,number),document.createTextNode(label));const grid=el("div","dns-record");grid.append(el("span",null,"Type"),el("code","mono",record.type),el("span",null,"Name"),el("code","mono",record.name),el("span",null,"Value"),el("code","mono",record.value));const copy=el("button","button ghost small","Copy value");copy.type="button";copy.onclick=()=>navigator.clipboard.writeText(record.value).then(()=>toast("DNS value copied.")).catch(()=>toast("Copy failed.",true));step.append(heading,grid,copy);return step}
function tlsStep(status){const step=el("div","dns-step");const heading=el("div","dns-step-label");heading.append(el("b",null,"3"),document.createTextNode("TLS · "+status));const copy=status==="active"?"Certificate is active at the edge.":status==="eligible"?"The edge may now issue and renew a certificate.":"TLS becomes eligible after ownership and routing are ready.";step.append(heading,el("p","tls-copy",copy));return step}
async function domainAction(domain,action){try{await api("/api/projects/"+encodeURIComponent(state.currentProject)+"/domains/"+encodeURIComponent(domain.id)+"/"+action,{method:"POST",body:{}});toast(action==="verify"?"Ownership checked. Routing status refreshed.":"DNS status refreshed.");await loadProject()}catch(error){toast(error.message,true)}}
async function removeDomain(domain){if(!confirm("Remove "+domain.hostname+" from this site?"))return;try{await api("/api/projects/"+encodeURIComponent(state.currentProject)+"/domains/"+encodeURIComponent(domain.id),{method:"DELETE"});toast("Domain removed.");await loadProject()}catch(error){toast(error.message,true)}}
function renderReleases(releases){const list=q("#release-list");clear(list);if(!releases.length){const row=el("tr");const cell=el("td","subtle","No deployments yet. Run clank deploy from your project.");cell.colSpan=5;row.append(cell);list.append(row);return}releases.forEach(release=>{const row=el("tr");row.append(el("td","mono",release.id.slice(0,12)),el("td",null,release.status),el("td",null,release.frameworkVersion),el("td",null,formatBytes(release.artifactBytes)),el("td",null,formatDate(release.createdAt)));list.append(row)})}
function renderLogs(logs){const list=q("#log-list");clear(list);if(!logs.length){list.append(el("div","chart-empty","Runtime logs will appear after the first deployment."));return}logs.forEach(log=>{const line=el("div","log-line");line.append(el("time",null,new Date(log.createdAt).toLocaleTimeString()),el("b",null,log.stream),el("span",null,log.message));list.append(line)});list.scrollTop=list.scrollHeight}
q("#new-site").onclick=()=>{if(!state.dashboard)return;const available=state.dashboard.organizations.some(org=>org.usage.projects<org.usage.limit);if(!available){toast("Every workspace has reached its site limit.",true);return}q("#site-error").textContent="";q("#site-dialog").showModal();q("#site-name").focus()};
q("#site-name").oninput=()=>{if(!q("#site-slug").dataset.edited)q("#site-slug").value=q("#site-name").value.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"").slice(0,48)};q("#site-slug").oninput=()=>q("#site-slug").dataset.edited="1";
q("#site-form").onsubmit=async event=>{event.preventDefault();q("#site-error").textContent="";try{const created=await api("/api/projects",{method:"POST",body:{name:q("#site-name").value,slug:q("#site-slug").value||undefined,organizationId:q("#site-organization").value}});q("#site-dialog").close();q("#site-form").reset();q("#site-slug").dataset.edited="";await loadDashboard(true);toast("Site created. Deploy from the CLI when ready.");await openProject(created.project.id)}catch(error){q("#site-error").textContent=error.message}};
q("#add-domain").onclick=()=>{if(state.projectData.domains.length>=state.projectData.domainLimit){toast("This site has reached its custom-domain limit.",true);return}q("#domain-error").textContent="";q("#domain-dialog").showModal();q("#domain-hostname").focus()};
q("#domain-form").onsubmit=async event=>{event.preventDefault();q("#domain-error").textContent="";try{await api("/api/projects/"+encodeURIComponent(state.currentProject)+"/domains",{method:"POST",body:{hostname:q("#domain-hostname").value}});q("#domain-dialog").close();q("#domain-form").reset();toast("Domain added. Publish the ownership and routing records.");await loadProject()}catch(error){q("#domain-error").textContent=error.message}};
qa("[data-close]").forEach(button=>button.onclick=()=>q("#"+button.dataset.close).close());q("#refresh").onclick=()=>state.currentProject?loadProject():loadDashboard();q("#mobile-menu").onclick=()=>q("#sidebar").classList.toggle("open");
showAuthenticated(initial.authenticated);updateAuth();if(initial.authenticated){loadDashboard(true);loadDevice();startRefresh()}
})();</script></body></html>`;
  return new Response(body, {
    headers: {
      "content-type": "text/html; charset=utf-8",
      "cache-control": "no-store",
      "content-security-policy": `default-src 'self'; script-src 'nonce-${nonce}'; style-src 'unsafe-inline'; connect-src 'self'; img-src 'self' data:; base-uri 'none'; form-action 'self'; frame-ancestors 'none'; object-src 'none'`,
      "x-content-type-options": "nosniff",
      "x-frame-options": "DENY",
      "referrer-policy": "no-referrer",
      "permissions-policy": "camera=(), microphone=(), geolocation=()",
      "cross-origin-opener-policy": "same-origin",
    },
  });
}

function randomNonce(bytes: number): string {
  const binary = Array.from(crypto.getRandomValues(new Uint8Array(bytes)), (byte) => String.fromCharCode(byte)).join("");
  return btoa(binary).replaceAll("+", "-").replaceAll("/", "_").replace(/=+$/u, "");
}
